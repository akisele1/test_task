Немного коментариев по результатам.
Задание 1.
- все полученные образы залиты в публичный доступ https://hub.docker.com/r/akisele1/test/tags?page=1&ordering=last_updated
  Отдельные проекты для каждого создавать не стал, разделил метками.
- все докерфайлы, которые требовались в задании лежат в этом репозитории в папке Docker_Part
Задание 2.
- разворачивал minikube. Разворачивал у себя на ноуте что бы задействовать виртуализацию с помощью Virtualbox. Пробовал с драйвером docker,
  но в официальной документации написано что этот драйвер экспериментальный и вернулся на виртбокс
- все требуемые YAML файлы по заданиям лежат в этом репозитории в папке Kubernates_part
Задание 3
- отдельно разделять YAML файлы не стал, лежит один финальный, который делает всё. Файл лежит в этом репозитории в папке Ansible_part
- Сам сервер ансибла поставлен в контейнер, который опять же залит на шару: akisele1/test_ansi:cent_ansi_host
- Особенностью данного файла состоит в том, что он работает с 2 хостами.
    Первый хост это опять же контейнер в который мы ставим kubectl и потом helm. (удобно было тестить сносишь конетейнер и по новой)
    И его опять же залил на шару akisele1/test_ansi:cent-ansi-sandbox
    разворот пакета хелм, проверка на работу сервиса и вывод контекста осуществляются на миникубе на моём ноуте (то что было развернуто для задания 2)
    что касается самого пакета helm, который разворачиваем, я его собрал из контейнеров и сервисов из пункта d задания по kubernates.
    Исходник и собранный пакет (хотя они по сути собраны из шаблона) лежат в этом же репозитории в папке mytest_ngunx_pack
    В ансибле старался использовать только основные модули, модули комьюнити не использовал, хотя там и есть модули под helm, kubernates и т.д.
Вроде всё, если нужно будет ещё что-то, готов оперативно залить на шару.
